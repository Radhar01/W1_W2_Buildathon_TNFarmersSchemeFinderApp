# --- Backend (FastAPI + RAG) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
chromadb==0.5.5
sentence-transformers==3.1.1
openai==1.51.0

# --- Frontend (Streamlit) ---
streamlit==1.38.0
requests==2.32.3

# Pin httpx so the OpenAI client keeps working (avoids the proxies error)
httpx==0.27.2
